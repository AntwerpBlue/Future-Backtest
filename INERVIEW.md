# Quant Researcher 面试题：CTA 因子挖掘

## 背景

本仓库提供了一套完整的中国商品期货因子挖掘与回测框架，覆盖从 1 分钟 K 线数据到因子分析的全流程。你的任务是基于此框架，独立完成因子研究并提交成果。

## 目标品种

请在以下三类品种上进行因子研究：

| 品种 | 代码 |
|---|---|
| 股指期货（IF） | `KQ.m@CFFEX.IF` |
| 国债期货（T） | `KQ.m@CFFEX.T` |
| 黄金（au） | `KQ.m@SHFE.au` |

## 数据区间

| 区间 | 年份 | 用途 |
|---|---|---|
| 样本内 | 2019 ~ 2022 | 因子开发、参数调优、IC/OLS/回测分析 |
| 样本外 | 2023 ~ 2025 | 最终验证，**禁止**用于参数调优 |

运行前请将 `config.json` 中的 `train_start_year` 设为 `2019`，`train_end_year` 设为 `2022`。样本外评估请使用 2023~2025 年数据独立验证。

## 因子时间粒度

因子可以基于以下两种 K 线粒度中的一种构建：

- **1 分钟 K 线**：通过 `bars["1m"]` 获取
- **5 分钟 K 线**：通过 `bars["5m"]` 获取

你可以根据因子逻辑自行选择粒度，也可以同时在两种粒度上构建不同因子。

## 预测目标

您可以在以下四个预测周期上分别评估因子表现：

| 预测周期 | 1 分钟 K 线 bar 数 | 5 分钟 K 线 bar 数 |
|---|---|---|
| 5 分钟 | 5 bars | 1 bar |
| 10 分钟 | 10 bars | 2 bars |
| 30 分钟 | 30 bars | 6 bars |
| 60 分钟 | 60 bars | 12 bars |

对应 `config.json` 中 `ic_forward_periods`：
- 1 分钟粒度：`[5, 10, 30, 60]`
- 5 分钟粒度：`[1, 2, 6, 12]`

## 评测标准

我们关注因子的**整体表现**，不以单一指标作为唯一评判标准。参考维度包括但不限于：

- **IC / ICIR**：因子预测方向的稳定性
- **OLS Beta t 值**：因子收益解释力的统计显著性
- **胜率**：回测中盈利交易的占比
- **PnL 与净值曲线**：资金曲线的稳定性和回撤控制
- **分层单调性**：Q1 ~ Q5 分层收益是否呈现单调递增/递减
- **多空价差**：Q5 与 Q1 层收益差异
- **跨品种泛化性**：因子在不同品种上的表现一致性

一个好的因子应当在多个维度上表现均衡，而非在某一指标上极端突出。评测基线可以参考factor_lab.ipynb中的值。

## 如何开始

1. **环境搭建**

   ```bash
   uv sync
   ```

2. **熟悉框架**

   详细的框架说明、特征工程、分析方法请参见 [README.md](README.md)。

3. **参考示例**

   `factor_lab.ipynb` 中包含一个完整的因子挖掘示例，涵盖 IC 分析、OLS 回归、资金曲线回测和分层分析的全流程。请以此为模板开展你的研究。

4. **编写因子**

   在 `factors/` 目录下新建你的因子文件（如 `factors/feat_yourname.py`），遵循框架的函数协议：

   ```python
   def feat_xxx(df: pl.DataFrame, bars: dict) -> Tuple[pl.DataFrame, List[str], List[str]]:
       ...
       return df, feature_names, categorical_feature_names

   PIPELINE = [('你的因子组', feat_xxx)]
   ```

5. **分析与验证**

   在 `factor_lab.ipynb` 中对你的因子进行交互式分析，记录你的研究过程和发现。

## 提交要求

将以下文件放入 `~/results` 目录：

```
~/results/
├── factors/your_factor.py     # 你的因子代码
└── factor_lab.ipynb           # 你的分析 Notebook（含运行结果）
```

请确保：
- 因子代码可以直接被框架加载运行
- Notebook 中保留完整的输出结果（图表、统计数据）
- Notebook 中包含你对因子逻辑和分析结论的简要说明

祝好运。
